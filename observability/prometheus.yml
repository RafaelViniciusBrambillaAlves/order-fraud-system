global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
    # Aqui o Prometheus busca as métricas que o servicos
    # enviaram ao collector (latência, erros, throughput, etc)